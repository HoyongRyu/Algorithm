<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rhy.sample.user.repository.UserMasterRepository">

	<insert id="insert" parameterType="rhy.sample.user.entity.User">
	    INSERT INTO tbas_user (
		    USR_ID,
		    AUTH_FG,
		    CRT_DT,
		    CRTR_ID,
		    MDFY_DT,
		    MDFR_ID
	    ) 
	    VALUES (
		    #{usrId},
		    #{authFg},
		    #{crtDt},
		    #{crtrId},
		    #{mdfyDt},
		    #{mdfrId}
	    )
	</insert>

</mapper>